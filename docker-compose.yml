services:
  bot:
    build: .
    container_name: gpn-fuel-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - gpn-data:/data
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }

volumes:
  gpn-data:
